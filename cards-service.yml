server:
  port: 0

spring:
  application:
    name: cards-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/cardsdb

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

logging:
  level:
    root: INFO
    com.banco.cards: DEBUG
